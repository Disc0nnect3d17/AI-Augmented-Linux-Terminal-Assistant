# AI-Augmented-Linux-Terminal-Assistant
AI-Augmented-Linux-Terminal-Assistant Cross-platform Electron desktop app embedding a real Linux shell via PTY and augmenting it with a locally hosted LLM. Captures structured session context to interpret command output, handle @ AI queries, and generate # scripts for manual review. No automated execution; full user control and safety enforced.
